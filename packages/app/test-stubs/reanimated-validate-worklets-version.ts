// Reanimated imports this CJS script with a default import. Unbundled, the browser cannot interop
// it, and the check it performs is a native-build concern with nothing to say in a browser test.
export default function validateWorkletsVersion(): void {}
